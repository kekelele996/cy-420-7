import { RoomStatus } from '../constants/room'; import type { Player } from './player'; import type { MapThemeId } from '../constants/mapThemes'; export interface Room { id:string; name:string; max_players:number; status:RoomStatus; map_size:{width:number;height:number}; map_theme:MapThemeId; players:Player[]; created_at:string }
