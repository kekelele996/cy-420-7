import type { Cell } from './cell'; import type { Player } from './player'; import type { MapThemeId } from '../constants/mapThemes'; export interface GameState { id:string; room_id:string; tick:number; map_theme:MapThemeId; map:Cell[][]; players:Player[]; leaderboards:{player_id:string;score:number}[]; start_time:string; end_time?:string }
